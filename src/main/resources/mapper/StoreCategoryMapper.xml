<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.dao.StoreCategoryDao">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.shop.entity.StoreCategory">
        <id column="id" property="id" />
        <result column="typ" property="typ" />
        <result column="name" property="name" />
        <result column="index" property="index" />
        <result column="sex" property="sex" />
        <result column="name_sign" property="nameSign" />
        <result column="typ_sign" property="typSign" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, typ, name, index, sex, name_sign, typ_sign
    </sql>

</mapper>
